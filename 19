#19) Crie um programa para preencher um vetor (lista) com números inteiros e solicitar um número do teclado. Pesquisar se esse número existe no vetor. Se existir, imprimir em qual posição do vetor (lista) foi digitado. Se não existir, imprimir mensagem que não existe.
vetor = []
tamanho = int(input("Quantos números deseja inserir na lista? "))

for i in range(tamanho):
    num = int(input(f"Digite o {i+1}º número: "))
    vetor.append(num)


numero = int(input("Digite um número para pesquisar na lista: "))


if numero in vetor:
    posicao = vetor.index(numero)
    print(f"O número {numero} foi encontrado na posição {posicao} da lista.")
else:
    print(f"O número {numero} não existe na lista.")
